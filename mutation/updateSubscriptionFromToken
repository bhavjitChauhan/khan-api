mutation updateSubscriptionFromToken($token: String!, $subscribe: Boolean!) {
  updateSubscriptionByToken(token: $token, subscribe: $subscribe) {
    error {
      code
      debugMessage
      __typename
    }
    __typename
  }
}