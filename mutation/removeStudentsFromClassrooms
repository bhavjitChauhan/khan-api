mutation removeStudentsFromClassrooms($kaids: [ID!]!, $classroomDescriptors: [String!]!) {
  removeStudentsFromClassrooms(
    kaids: $kaids
    classroomDescriptors: $classroomDescriptors
  ) {
    coach {
      id
      kaid
      __typename
    }
    __typename
  }
}