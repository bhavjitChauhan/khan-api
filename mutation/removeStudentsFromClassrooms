mutation removeStudentsFromClassrooms($kaids: [ID!]!, $classroomIds: [ID!]!) {
  removeStudentsFromClassrooms(kaids: $kaids, classroomIds: $classroomIds) {
    coach {
      id
      kaid
      __typename
    }
    __typename
  }
}
