mutation passwordChangeMutation($kaid: String!, $password: String!, $oldPassword: String) {
  passwordChange(kaid: $kaid, password: $password, oldPassword: $oldPassword) {
    error {
      code
      __typename
    }
    __typename
  }
}