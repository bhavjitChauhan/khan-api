mutation classroomProgressReset($classroomDescriptor: String!) {
  classroomProgressReset(classroomDescriptor: $classroomDescriptor) {
    error {
      code
      __typename
    }
    __typename
  }
}
