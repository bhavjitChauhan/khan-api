mutation addStudentToAssignment($id: ID!, $studentKaid: String!) {
  addStudentToAssignment(id: $id, studentKaid: $studentKaid) {
    success
    error {
      code
      __typename
    }
    __typename
  }
}