mutation UpdateSeatCount($subscriptionID: ID!, $quantity: Int!) {
  updateKadssSeatCount(subscriptionID: $subscriptionID, quantity: $quantity) {
    error {
      code
      __typename
    }
    __typename
  }
}