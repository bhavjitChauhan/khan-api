mutation sendTestRealTimeMessage($channel: String!, $message: String!) {
  sendTestRealTimeMessage(channel: $channel, message: $message) {
    error {
      code
      __typename
    }
    __typename
  }
}