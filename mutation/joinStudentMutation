mutation joinStudentMutation($classCode: String!) {
  joinStudent(classCode: $classCode) {
    errors
    error {
      code
      signupCode
      classroomName
      __typename
    }
    classroom {
      id
      cacheId
      signupCode
      name
      __typename
    }
    __typename
  }
}