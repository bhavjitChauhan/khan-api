query CourseIntroContent($descriptor: String!) {
  learnableContentByDescriptors(contentDescriptors: [$descriptor]) {
    id
    defaultUrlPath
    contentKind
    ... on Video {
      duration
      thumbnailUrl
      __typename
    }
    __typename
  }
}
